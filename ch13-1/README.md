# 실습과제 1

- 배열명의 의미 : 배열 첫번째 요소의 주소

- 배열표현과 포인터(주소) 표현의 변환 공식 :
  `arr[i] = *(arr + i)`

- 배열의 첫번째 요소의 주소만 알면 모든 원소의 주소를 계산할 수 있다.
  : 선언된 배열의 자료형 크기를 이용하여 각 원소의 주소를 계산할 수 있다.

- 배열의 첫번째 요소의 주소만 알면 모든 원소의 값을 계산할 수 있다.
  : `*(arr + i)`를 사용하여 모든 배열 값에 접근할 수 있다.

- 포인터에 대해 가능한 연산의 종류와 의미 :
  - `p + n` → n칸 뒤로
  - `p - n` → n칸 앞으로
  - `p++` → 다음 요소
  - `p--` → 이전 요소

- 포인터 연산을 기존의 산술연산과 다르게 정의한 이유는 무엇일까?
  : 포인터 연산은 포인터가 가리키는 자료형의 크기를 고려하여 주소를 이동해야 하기 때문에 일반적인 산술연산과 다르게 정의된다.
