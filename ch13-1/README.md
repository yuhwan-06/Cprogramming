# 실습과제 1

- 배열명의 의미 : 배열 첫번째 요소의 주소

- 배열표현과 포인터(주소) 표현의 변환 공식 :
  `arr[i] = *(arr + i)`

- 배열의 첫번째 요소의 주소만 알면 모든 원소의 주소를 계산할 수 있다.
  : 선언된 배열의 자료형 크기를 이용하여 각 원소의 주소를 계산할 수 있다.

- 배열의 첫번째 요소의 주소만 알면 모든 원소의 값을 계산할 수 있다.
  : `*(arr + i)`를 사용하여 모든 배열 값에 접근할 수 있다.

- 포인터에 대해 가능한 연산의 종류와 의미 :
  - `p + n` → n칸 뒤로
  - `p - n` → n칸 앞으로
  - `p++` → 다음 요소
  - `p--` → 이전 요소

- 포인터 연산을 기존의 산술연산과 다르게 정의한 이유는 무엇일까?
  : 포인터 연산은 포인터가 가리키는 자료형의 크기를 고려하여 주소를 이동해야 하기 때문에 일반적인 산술연산과 다르게 정의된다.

# 실습과제 2

```c
#include <stdio.h>
int main(void)
{

int grade[5];

int sum = 0, i, average;

for(i = 0; i < 5; i++)
{
    printf("성적을 입력하시오: ");
    scanf("%d", grade + i);
}
for(i = 0; i < 5; i++)
    sum += *(grade + i);

average = sum / 5.0;
printf("성적 평균= %d\n", average);

return 0;
}
```

# 실습과제 3

- 증감연산자 a++, ++a의 차이를 설명하시오. : a++ 는 후위 연산자로 연산 후 값을 증가시키고, ++a 는 전위 연산자로 선 증가 후 연산을 한다.

```c
#include <stdio.h>
int main(void)
{
int a[] = { 10, 20, 30 }, i, sum = 0;
for (i = 0; i < 3; i++)
  //sum += *a++; 고정되어 있는 주소값을 임의로 연산을 통해 변형 하려 했기때문에 문제 발생
  sum ++ *(a + i) // 수정결과
printf("sum:%d\n", sum);
return 0;
}
```
